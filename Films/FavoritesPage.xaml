<UserControl x:Class="Films.FavoritesPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Films"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <ListBox x:Name="filmsList" SelectionChanged="filmsList_selection">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Margin="5">
                        <TextBlock Binding="{Binding Path = Title}" />
                        <TextBlock Binding="{Binding Path = Runtime}" />
                        <TextBlock Binding="{Binding Path = YearOfFilm}"/>
                        <Image Width="64" Source="{Binding Path = Poster}" />
                        <TextBlock Binding="{Binding Path = Genre}" />
                        <TextBlock Binding="{Binding Path = Writer}"  />
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</UserControl>
